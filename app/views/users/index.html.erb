<% @users.each do |user| %>
	<%= user.id %>
	<%= user.admin %>
	<%= user.name %>
	<%= user.surname %>
	(<%= user.email %>)
	<% if admin? %>
	
	<%= link_to "Izbriši", user, :method => :delete,
:confirm => "A si ziher?"
%> 
<% end %>
<% if correct_user?(user) %>
<%= link_to "Uredi", edit_user_path(user) %> 
<% end %>

	<br />
<% end %>
